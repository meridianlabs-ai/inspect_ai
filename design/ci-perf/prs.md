# PRs opened by the ci-perf skill

Permanent ledger of every change and PR produced by the ci-perf skill
(`.claude/skills/ci-perf/SKILL.md`). Appended/updated each run — never
rewritten. One entry per PR: number, date opened, what it changed, status,
and measured impact once a later snapshot verifies it.

| PR | Opened | Change | Status | Measured impact |
|---|---|---|---|---|
| [#4746](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4746) | 2026-08-04 | Add `--durations=50` to Build pytest for per-test timing visibility | merged | Enabled the durations data all later runs analyze |
| [#4747](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4747) | 2026-08-04 | Remove `changes` → `test` serialization from Build critical path | merged | Verified 2026-08-05: full queue+start cycle removed from the longest job's critical path |
| [#4748](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4748) | 2026-08-04 | The ci-perf skill itself (collector script + SKILL.md) | merged | n/a (tooling) |
| [#4760](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4760) | 2026-08-05 | Two slow-test fixes (38s sandbox-init test, S3 pagination test), `filter: blob:none` checkouts, pre-install of tests/test_package, slow-test policy docs | merged | Verified 2026-08-12: −68s `test` exec, −20–25s each shorter job; erratic 30s–4min fetches gone (checkout now 4–5s median); predicted −60s, held |
| [#4848](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4848) | 2026-08-12 | Mark 43s docker `read_file` test slow, `blob:none` on the Viewer `check-schema-and-types` checkout, collector hardening (dedupe/sort/gap warning) | merged | Verified 2026-08-18, mixed. Viewer half held: `check-schema-and-types` 74→54s median, checkout max 216→16s, Viewer wall 82→67s. Test half missed: predicted −15–40s on `test` exec, measured 442→445s — the 45s it removed from the durations tail (194→150s per job) was fully absorbed by six days of suite growth |
| [#4932](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4932) | 2026-08-18 | Scheduled unattended mode, suite-size analysis (`pytest_summaries` in the collector), this ledger; companion workflow [meridianlabs-ai/actions#93](https://github.com/meridianlabs-ai/actions/pull/93) | merged | n/a (tooling) — its `pytest_summaries` produced the suite-size baseline in the 2026-08-18 report |
| [#4935](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4935) | 2026-08-18 | Report + snapshot for 2026-08-18 (first fully unattended run) plus its two prepared safe fixes: `pytest -n logical` in the `test` job (measured 449→334s) and `blob:none` on the last 4 full-history checkouts (~75s off the sandbox-tools chain) | merged 2026-08-19 | Verified 2026-08-19, both halves held. Predicted `test` exec 445 → ~330s and Build wall 485 → ~370s; measured, splitting this window's legs on whether the branch carried the fix: pytest step 417 → 320s, `test` exec 447 → 349s (−98s), Build wall 485 → 383s (−102s). `blob:none`: `check-version-bump` checkout 27.5 → 5s, `slow-tool-tests-dev` 29.5 → 6s; `slow-tests` and `slow-tool-tests-release` didn't run post-merge, still unverified — **`slow-tests` closed out 2026-08-21: 7 runs, checkout 56 → 6s median (p90 13.8s); `check-version-bump` job exec 30 → 8s. `slow-tool-tests-release` has still never executed in any window, so that leg stays unverified indefinitely.** No OOM or worker deaths at 4 workers. Second-order effect the prediction missed: 4 workers made xdist scheduling imbalance the new long pole (see #261). The run's fine-grained token couldn't push workflow files or reach upstream (report proposal 3), so it opened fork PR [meridianlabs-ai/inspect_ai#255](https://github.com/meridianlabs-ai/inspect_ai/pull/255) (closed); a maintainer took over the branch and opened this PR |
| [#4948](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4948) | 2026-08-19 | `--dist worksteal` in the pytest `addopts` (stops one xdist worker stranding 76–80s of work while its peers idle), the 2026-08-19 report + snapshot + this ledger, and a skill change: each run now ships its entire output as a single PR | merged 2026-08-19 | Verified 2026-08-21: **mechanism confirmed, magnitude missed by ~40%**. Classifying every Build run by whether its head commit's history contains the fix (compare API, not wall-clock time): binding (slower) test leg exec 387 → **344s**, slower leg's pytest step 356 → **313s**, Build wall on plain PRs 383 → **345s**, pytest-reported wall across all legs 311 → **291s** — against a predicted 71s off Build wall, so ~38–43s delivered. Worker balance is exactly as predicted and the failure mode is gone: eight post-merge CI legs show imbalance +3–6s (1–3%) at 97–99% worker efficiency against +12–80s at 68–88% before, zero stragglers in eight legs, and Build runs whose two legs differ by >60s fell from 21% (13/61) to 6% (3/47). Why the estimate ran high: 71s was the median of `max(leg wall) − leg avg`, which is an upper bound (perfect rebalancing does not drive the slowest worker all the way to the average), computed on a five-run sample where 4 of 10 legs happened to carry a straggler — a 40% rate against ~21% over the wider sample. Caveats: the `load` arm is only 4 runs / 8 legs, and two days of suite growth (+123 collected items) push against worksteal, so 43s is slightly conservative. Original prediction: 71s median off Build wall clock. Evidence: 4 of 10 post-#4935 CI legs had a +76–80s straggler; fix validated over five local full-suite runs — imbalance +22s/+83s under `load` against +4s/+7s/+7s under `worksteal` (worker efficiency 72–88% → 96%), identical results throughout — and in a one-run-per-arm CI A/B (fork PRs #261 vs #262): worksteal legs +3s/+4s at 95% efficiency, load legs +24s/+80s at 68–86%, a 72s difference in slower-leg test-phase wall. History: the run's token couldn't reach upstream, so it opened fork PRs [#261](https://github.com/meridianlabs-ai/inspect_ai/pull/261)/[#262](https://github.com/meridianlabs-ai/inspect_ai/pull/262); a maintainer promoted them (#4946/#4947, closed) and combined them into this PR |
| [meridianlabs-ai/inspect_ai#300](https://github.com/meridianlabs-ai/inspect_ai/pull/300) | 2026-08-21 | The 2026-08-21 report + snapshot + this ledger. The scheduled run itself shipped no code fix — every remaining wall-clock item lives in `.github/workflows/**` or `.claude/**`, neither writable by the scheduled token (re-probed on the git path, the REST Contents API and upstream PR creation — all 403) — and filed three structural proposals as issues: [#297](https://github.com/meridianlabs-ai/inspect_ai/issues/297) (cache the Quarto render), [#298](https://github.com/meridianlabs-ai/inspect_ai/issues/298) (unblock the scheduled run), [#299](https://github.com/meridianlabs-ai/inspect_ai/issues/299) (exclude `design/**` from the `test` filter). **A maintainer then implemented #297 and #299 directly in this same PR** (commits `7992b1ce8`, `640577ebd`): the `docs` job skips its 315s Quarto render on an exact-input-set cache hit (marker keyed on `hashFiles('docs/**', 'requirements-doc.txt', 'src/inspect_ai/**')`, saved only after a successful render), and `design/**` no longer counts as code for the `test` matrix. #298 remains open | closed 2026-08-21, promoted upstream as [#4995](https://github.com/UKGovernmentBEIS/inspect_ai/pull/4995) (merged `af62b3fbb`) | **Half verified 2026-08-23.** Neither fix was exercised inside that run's snapshot window: of the 20 Build runs that started after the 18:08Z merge, every `docs` job was skipped (no PR touched `docs/**`), so the render-cache step appears in 0 of the window's 13 `docs` job records, and no design-only push occurred. **`design/**` (#299) then verified on the next run's own PR** ([#312](https://github.com/meridianlabs-ai/inspect_ai/pull/312), a design-only push): `test` legs 4s each instead of ~332s, Build wall 96s, 6.9 runner-min against a predicted 6–7 — exact hit. **The Quarto render cache (#297) verified 2026-08-25: the mechanism works per hit, the hit rate is ~8%.** 16 `docs` jobs ran the cache step; one hit, at **12s against a 375s median** (and that run's Build wall was 347s against 390s for the same branch's neighbouring pushes) — so the per-hit prediction held exactly. But GitHub's own cache index shows 12 `docs-render-*` entries with exactly one ever re-used, and PR 4884 alone created four keys in 26h: on `pull_request` the checkout is the *merge* ref, so the key hashes the PR merged into current `main`, and every push to `main` touching `src/inspect_ai/**` (8 of this window's 10 main commits) invalidates every open PR's entry. `Render docs` is still 314s median. Follow-up fix filed as [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317) |
| [meridianlabs-ai/inspect_ai#312](https://github.com/meridianlabs-ai/inspect_ai/pull/312) → [#5057](https://github.com/UKGovernmentBEIS/inspect_ai/pull/5057) | 2026-08-23 | The 2026-08-23 report + snapshot + this ledger. **No code fix again** — all three blockers re-probed and still standing: pushing a `.github/workflows/**` edit to the fork is rejected for want of `workflow` scope (probe branch created and deleted), upstream PR creation returns 403, and `.claude/**` is unwritable in the sandbox. New detail: `.github/workflows/**` *is* writable in the sandbox, so only a probe branch reveals blocker 1. Evidence added as a comment on [#298](https://github.com/meridianlabs-ai/inspect_ai/issues/298). One new structural proposal filed: [#311](https://github.com/meridianlabs-ai/inspect_ai/issues/311) — `import inspect_ai` is 1.85s and `acp.schema` is 476ms of it (26%, six times the next-largest module), reached through two eager edges; `inspect --version` takes 2.05s | closed 2026-08-26, promoted upstream by a maintainer as [#5057](https://github.com/UKGovernmentBEIS/inspect_ai/pull/5057) (merged `abf5952b6`, 2026-08-26 14:57Z). **The 2026-08-25 run pushed its output onto this same branch** rather than opening a second PR, per the skill's unattended rules: the 2026-08-25 snapshot, a full report rewrite and this ledger row | n/a (report). The 2026-08-23 run's measurements: **#4987 verified** (~12.5 min off the one `slow-tool-tests-release` execution, matching its ~13 min estimate); the 2026-08-21 workflow fixes (#297 docs cache, #299 `design/**` filter) had **zero observations** at the time. This PR is itself design-only and became the first observation of #299: its `test (3.10)` / `test (3.11)` legs ran **4s each** instead of ~332s, Build wall **96s** against a 342s code-only median, and the whole push cost **6.9 runner-min** (Build 4.1 + Viewer 2.8) against the predicted 6–7 — prediction hit exactly. **The 2026-08-25 run added:** #297 measured at last (see its row above — hits ~8%, filed [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317)); a viewer component-test regression filed as [#318](https://github.com/meridianlabs-ai/inspect_ai/issues/318) (39s → 55s, +42%, since the #4934 `ts-mono` bump); still **no code fix** (fifth consecutive run), with one blocker corrected — `.claude/**` is writable in the sandbox and it is the agent's edit tooling that refuses the path, so the fix is a harness permission-policy change, not a token one ([#298](https://github.com/meridianlabs-ai/inspect_ai/issues/298) updated) |
| [meridianlabs-ai/inspect_ai#343](https://github.com/meridianlabs-ai/inspect_ai/pull/343) | 2026-08-27 | The 2026-08-27 report + snapshot + this ledger. **No code fix — sixth consecutive run**, all three blockers re-probed with real attempts today (a probe branch carrying a one-byte `.github/workflows/build.yml` edit was rejected for want of `workflow` scope; a plain write under `.claude/skills/ci-perf/` was refused by the agent's edit tooling; upstream PR creation returned 403 again). The run's headline finding is new and large: **`-rA` costs ~46s per `test` leg — 14% of the pytest step** — printing `Captured stdout call` blocks (~31,000 lines per leg) for tests that *passed*; `-ra` keeps every failure/error/skip/xfail report and drops only that. Measured by timestamp from the raw job logs of 16 test legs; filed as [#342](https://github.com/meridianlabs-ai/inspect_ai/issues/342) rather than shipped, because the two places carrying the flag are `.github/workflows/build.yml` and `pyproject.toml` and the workflow half cannot be pushed. The run also closed out two open questions: the `test`-leg creep is **suite growth plus noise** (+674 collected items in two days, against a newly measured ±60s-of-worker-time / ±8% noise floor), and the viewer regression [#318](https://github.com/meridianlabs-ai/inspect_ai/issues/318) **resolved itself** at the `ts-mono` bump `125db36a5`→`f2057d95f` (54→40s, Viewer wall 90→71s) | closed 2026-08-27, promoted upstream by a maintainer as [#5075](https://github.com/UKGovernmentBEIS/inspect_ai/pull/5075) (merged `b17ff6159`, 2026-08-27 14:02Z) — **and the maintainer carried the `-rA` → `-ra` fix in with it**, so the report's proposal 1 shipped after all, in `.github/workflows/build.yml` and `pyproject.toml`; [#342](https://github.com/meridianlabs-ai/inspect_ai/issues/342) closed | **Verified 2026-08-29 — prediction hit.** The `-ra` half: `Test with pytest` 324.5 → **292.5s** (3.10) and 323 → **301s** (3.11), pytest's own reported wall 328.7 → **287.1s**, Build wall on successful code-only pushes 369.5 → **341s** (p90 434 → 353s), raw `test` job log ~31,000 → **5,477 lines**. Predicted ~46s off the step; measured −41.6s of pytest wall while the suite grew 550 collected items (~+4s) in the same two days, i.e. ~46s gross. Measured worker time is unchanged (841.9 → 846.2s across matched `--report-log` artifacts), so the saving came out of the reporting phase rather than out of test execution — exactly the claimed mechanism. Verification recorded on #342. Also verified this run: **#297** docs render cache 2 hits in 19 docs jobs (11%, was 1/13) — both hits consecutive pushes to one branch ~8 min apart, evidence added to [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317); **#299** `design/**` exclusion holding (3 design/docs-only runs at 122s median Build wall against 368s code-only); **#4948** `worksteal` holding at five windows (+3..+9s imbalance, 96–99% efficiency, 12 legs); **#4760** `test_package` pre-install survives `uv run`'s re-sync (all `test_extensions` tests ≤0.3s, against the ~9s a mid-run install costs) ; and this PR is itself the fourth observation of **#299** — `test (3.10)` 6s, `test (3.11)` 3s, Build wall **102s** against a 368s code-only median, 7.2 runner-min for the whole push, with `mypy` (87s/89s) now the long pole of a design-only push |
| [meridianlabs-ai/inspect_ai#375](https://github.com/meridianlabs-ai/inspect_ai/pull/375) | 2026-08-29 | The 2026-08-29 report + snapshot + this ledger. **No code fix — seventh consecutive run**, with all three blockers re-probed by real attempts today: a probe branch carrying a one-byte `.github/workflows/build.yml` edit was rejected for want of `workflow` scope while a control push of a non-workflow file on the same branch succeeded (both probe branches deleted); a plain write under `.claude/skills/ci-perf/` was refused by the agent's edit tooling as a "sensitive file"; and `POST /repos/UKGovernmentBEIS/inspect_ai/pulls` with `head_repo` returned 403 again (`repos/UKGovernmentBEIS/inspect_ai` reports `push: false` for this token). The one shippable candidate the run evaluated — `compile-bytecode` in `pyproject.toml`, which needs no `workflow` scope — was **measured and rejected**: +13.6s on `uv pip install .[dev]` for −3.5s on collection, a net loss of ~10s per leg. Two new findings: (1) the 55.3s of pytest startup is **assertion rewriting, not bytecode compilation** — `compileall` over `src`+`tests` runs in 1.3s and leaves cold collection unchanged (36.2 → 33.4s) because pytest keeps its own `*-pytest-9.1.1.pyc`, deleting only those reproduces the full cold cost (32.6s), and `--assert=plain` cuts it to 14.9s against a 12.7s warm run — which corrects the attribution the 2026-08-25 and 2026-08-27 reports carried; (2) rendering `EvalError.traceback_ansi` costs **~60s of worker time per `test` leg** (~15s of leg wall, 5% of test-execution worker time) and 0.3–4s of CPU per distinct error for real users, measured by instrumenting `format_traceback` across two full local suite runs (649 calls, ~250 uncached, 208 tests; the two Google bridge tests pay 3.9s *each*) — filed as [#374](https://github.com/meridianlabs-ai/inspect_ai/issues/374). The report also reframes suite-size policy on measured data: 13,307 tests under 0.1s are 8.4% of test time while 1,221 tests in the 0.1–1s band are 50.8%, so the question is how many tests call `eval()`, not how many tests exist. **The 2026-08-31 run pushed its output onto this same branch** (per the unattended rule that an open predecessor PR is extended rather than duplicated): the 2026-08-31 snapshot, a full report rewrite, this ledger row — and **the first code fix this skill has shipped in eight runs**. `clear_model_info_cache()` reset four globals, two of them the *immutable* built-in model database and its lookup index; an autouse fixture in four test files calls it before and after every test, so **191 tests each re-read and re-parsed the model DB** at ~0.19s a time. Nothing in `src/` calls the function (five test files are its only users) and nothing mutates the DB (`set_model_info` / `set_model_cost` write to the custom registry, the latter via `model_copy`), so keeping it costs no isolation. Measured: the affected files go **49.9s → 31.9s** locally (205 tests, single process, 3.11; `tests/model/test_model_info.py` alone 11.02 → 2.27s), and in today's CI report logs those files cost **73.5s (3.10) / 55.5s (3.11)** of worker time. Validation: `ruff check`, `ruff format --check`, repo-wide `mypy`, and the full local suite (10,443 passed, 4,507 skipped, 0 failures). **Verified in CI within the hour, prediction beaten**: because the fix touches `src/`, this PR's own `test` legs ran, and diffing their `--report-log` artifacts against upstream run 33343746910 gives the four affected files at **73.5 → 29.2s (−44.3s) on 3.10** and **55.5 → 28.9s (−26.6s) on 3.11**, against a predicted −25s / −18s; `test_model_info.py` 23.6 → 0.7s, `test_model_family.py` 7.4 → 0.2s, `test_canonical_names.py` 5.1 → 0.5s, `test_sample_limits.py` 37.5 → 27.9s. Whole-leg worker time 892.2 → 839.1s (3.10) and 903.0 → 843.4s (3.11), the residual being inside the ±60s noise band | closed 2026-09-02, promoted upstream by a maintainer as [#5196](https://github.com/UKGovernmentBEIS/inspect_ai/pull/5196) (merged `b30a4f539`) | n/a (report). **The model-info fix it carried is verified on `main` as of 2026-09-03, prediction beaten a second time**: in upstream run 33736817639's report log the four affected files cost **30.8s against 73.5s pre-fix** — `tests/model/test_model_info.py` **0.71s over 51 tests** (was 17.4s over 47), `test_model_family.py` **0.13s** (7.4s), `test_canonical_names.py` **0.49s** (5.1s), `test_sample_limits.py` **29.5s** (37.5s). The pre-merge fork-branch measurement (29.2s) held to within 1.6s on a suite that grew 675 collected items in between. Verified on the earlier runs: **#5075** `-ra` (see the row above — prediction hit, ~46s); **#297** docs render cache still ~11% (2 of 14 docs jobs), with the cache API showing 55 `docs-render-*` entries over 38 distinct keys of which only **4 were ever re-read** and 16–22 new keys/day — evidence added to [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317), which is now the top-ranked lever because `docs` has become the binding job class (392s exec; docs-touching pushes 406–422s of Build wall against 341s code-only, binding in 11 of 59 runs); **#299** `design/**` exclusion holding (4 design-only pushes bound by `mypy` at 153.5s Build wall); **#4948** `worksteal` holding at six windows (+5.8s imbalance, 97.3% efficiency); **#4760** `test_package` pre-install holding (no `test_extensions` test anywhere in the durations tail). Queue remains a non-issue: median 3s, p90 8s, max 45s, zero waits over 60s in 820 job records. This PR is itself the **fifth observation of #299**: `test (3.10)` and `test (3.11)` ran **4s each** instead of ~330s, Build wall **100s** against a 341s code-only median, and the whole push cost **7.5 runner-minutes** (Build 4.5 + Viewer 3.0), with `mypy` (84s/96s) again the long pole of a design-only push. All checks green; two CI iterations were needed because the `typos` pre-commit hook rejects that hyphenated spelling of "misattributed" (it reads the prefix as a word). **The 2026-08-31 run added:** **#4935's last unverified leg closed ten windows later** — `slow-tool-tests-release` had never executed in any snapshot and ran 6 times this window with a **5–8s checkout** (against 27–30s full-history checkouts on its siblings before the change); all 6 failures are the published-binary gate failing fast at 31–49s, by design. **#297 is getting worse, not better**: 1 hit in 19 docs jobs (**5%**, after 14/11/8%), and the cache index now shows **14 of 39 distinct `docs-render-*` keys existing twice — once under a PR merge ref and once under `refs/heads/main`** — ~76 runner-minutes of provably identical renders in 5 days, and proof that the current key design has a ceiling (a PR-scoped entry can never be read by `main`); posted to [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317), which is now the top lever by a wide margin (`docs` binds 13 of 49 Build runs **by a 105s median margin**, and docs-touching pushes are 401–405s of wall against 337s code-only). **#5075** holding (pytest wall 287.1 → 290.3s across +277 collected items; reporting phase 0.2s; leg log 5,493 lines). **#299** holding (two design/md-only pushes at **90s** Build wall). **#4948** holding at seven windows (+5.4s / +3.2s imbalance, 97.7% / 98.6% efficiency). Queue still a non-issue (median 3s, p90 8s, max 63s over 847 records, one wait above 60s) — seventh window with no support for the runner-pool proposal. One new report finding: `tests/util/test_display_counter.py` sleeps **6 × 1.1s** (8.9–10.5s of worker time) for a 1 Hz footer throttle, and two of the six `DisplayType` params resolve to displays whose status print is `@throttle(5)`, so their sleep cannot fire anything. The collector's stale-page bug bit a **fourth** time (attempt 1 served a 191h window with a 77h hole while a direct `gh api` call showed runs 30 hours newer); its one-file fix remains uncommittable under `.claude/**` **The 2026-09-01 run added:** the largest finding of the series so far — **every `eval()` spends ~214ms starting and stopping the uvicorn control-channel server** (a one-sample `mockllm` eval is 249ms with it, 35ms without; the split is 30ms rebuilding an identical 28-route FastAPI app, 30ms binding, and **100ms waiting out uvicorn's fixed 0.1s `should_exit` poll**). 796 test functions across 153 files call `eval()`/`eval_set()` directly, and a full-suite A/B on 4 workers — one autouse fixture forcing the server off as the only difference — runs **725.8 → 529.0s of wall (−27%)** and **697.6 → 512.3s of test worker time**, with **381 tests leaving the 0.1–1s band** that owns 49% of CI test time. Only **14 tests fail** without it, all with the control channel as their subject (8 `_control/test_launch_handoff.py`, 5 `_control/test_eval_set_integration.py`, 1 `test_eval_set_selection_parks_for_keep_alive`). Filed as [#393](https://github.com/meridianlabs-ai/inspect_ai/issues/393) rather than shipped: both candidate fixes (cheaper start/stop, or a suite default with a `@pytest.mark.real_ctl_server` opt-out) are maintainer decisions, not mechanical ones — **so this run ships no code fix**. **#297 hit 0 of 5 docs jobs** (after 14/11/8/5%), and the cache index has roughly doubled its duplicate evidence: 107 `docs-render-*` entries over 77 distinct keys with **4 ever re-read**, and **30 keys existing twice — once under a PR merge ref, once under `refs/heads/main`** (~165 runner-min of identical renders in 7 days); even the same-branch re-push case that produced every earlier hit now misses. Posted to [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317). A **new `Suppressions` workflow** (upstream #5136, landed 2026-08-31 15:32Z) now runs on every PR: 16s exec, 20s wall, 13 runner-min per 200 runs, per-push compute 17.6 → 18.5 min. **#5075** holding (reporting phase 1.1s, leg log 5,555 lines); **#4948** holding at eight windows (+4.2s imbalance, 98.1% efficiency); **#4760** and **#4935** holding; **#299** had no observation (no design/md-only push in the window). The model-info fix on this branch is **still unlanded** and still worth what it measured — upstream run 33473874133 shows `tests/model/test_model_info.py` at the pre-fix 17.4s of worker time. Queue a non-issue for the eighth window, across the densest hour the series has seen (64 runs, 237 independent-job records, 3s median, 69s max). The collector did **not** misfire this run, but a second collector problem surfaced: at 19.6 runs/hour a 200-run snapshot spans only **10.2h**, so **17.3h between this window and the last is uncovered by either snapshot** |
| [meridianlabs-ai/inspect_ai#408](https://github.com/meridianlabs-ai/inspect_ai/pull/408) | 2026-09-03 | The 2026-09-03 report + snapshot + this ledger, and **the first workflow-file change this skill has shipped** — because the standing `workflow`-scope blocker cleared this run. Re-probed with a real attempt: a probe branch carrying a one-byte `.github/workflows/build.yml` edit pushed to the fork successfully and the blob was confirmed changed on the remote (branch deleted afterwards), where every run from 2026-08-21 through 2026-09-01 was rejected. The other two blockers stand — upstream `POST /pulls` with `head_repo` returned 403 again (`push: false` for this token) and a write under `.claude/skills/ci-perf/` was still refused as a sensitive file. **Fix 1 (workflow):** the `test` and `mypy` jobs installed with `uv venv` + `uv pip install .[dev]` — an *unlocked* resolution — and then ran under `uv run`, which syncs the project environment to `uv.lock` first, undoing most of it. Measured from raw job logs: `uv run pytest` rebuilds `inspect_ai`, re-downloads 12 packages, uninstalls 46 and installs 48 (**5.3s**, job 100589231813); `uv run mypy` does the same (**3.5s**, job 100589231638). Replaced with `uv sync` + `uv run --no-sync`; the environment is provably unchanged (`uv pip freeze` after either sequence gives byte-identical 257-package lists), and `tests/test_package` moves after the sync so #4760's pre-install survives. Predicted **~5s off Build wall** on the ~70% of runs where a `test` leg binds (13–20s margins) and ~18s of runner time per push. Deliberately not applied to the other five `uv run` sites: for `docs` and `sandbox-tools-unit` it would change which dependency versions the job tests against, and `slow-tool-tests-release` *relies* on the sync swapping its wheel install for the lockfile's editable one so `_binaries_dir()` resolves to the source tree the published binaries were downloaded into. **Fix 2 (test):** the duplicate-coverage sweep found `test_agent_as_tool_no_param_docs_error` and `test_agent_handoff_no_param_docs_error` both calling `check_agent_as_tool_no_docs_error`, so they were byte-identical to the two tests above them and `check_agent_as_tool_no_param_docs_error` — the only assertion on the "Description not provided for parameter" path — was unreferenced and never ran; rewired (zero wall-clock impact, one error path recovered). Validation: `ruff check`, `ruff format --check`, repo-wide `mypy` (1444 files), the suppressions gate, and the full local suite (11,188 passed / 4,702 skipped / 1 failed — `test_anthropic_fable_5_1_no_binding_off_first_party[azure/...]`, which reproduces on unmodified `origin/main` in this sandbox and is caused by `ANTHROPIC_BASE_URL` being set here; CI is green on `main`). One fresh-context Opus 5 review pass: 0 correctness defects, 2 comment/accuracy fixes applied, 1 dismissed. Issue evidence posted to [#298](https://github.com/meridianlabs-ai/inspect_ai/issues/298) (scope blocker cleared), [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317) (docs cache still 0 hits; index 165 entries / 129 keys / 3 ever re-read / 36 keys duplicated across a PR ref and `main`; **now unblocked but deliberately unshipped** because every candidate key trades render correctness for hit rate) and [#308](https://github.com/meridianlabs-ai/inspect_ai/issues/308) (**closed, but its fix is silently undone by `uv run`'s implicit sync** — 14 packages differ between the injectable's own pins and the root lockfile the job actually gets, including `mcp 2.1.1 → 2.0.0`, the exact package that issue was filed about) | open (fork; needs maintainer promotion upstream — 403 again) | Pending — the `uv sync` prediction (~5s off Build wall, ~18s of runner time per push) is checked against the next snapshot. **New findings this run, for the record:** (1) **queue stopped being a non-issue** after eight windows — p90 23 → **57s**, max 69 → **157s**, waits over a minute 8 → **65** — all from one 16-minute burst of 47 runs across 6 branches, during which code-only Build wall was **418s against 350s outside it (+68s)** while `test`-leg execution was flat (333/331s vs 335/337s), so the whole delta is queue; it is *not* a hard cap (concurrency reached 35 earlier the same evening, and 24 runs/10min the next morning held a 3s median), so this is ramp latency and "buy more runners" still has no support. (2) The collector's `event=pull_request` filter hides **319 runner-minutes and 376 job records** per window — the 12 push-event Build runs are the single largest compute line — so real load here is **1,270 runner-min over 12.5h**, load was understated ~25%, and every concurrency figure in every prior report is a lower bound. (3) Suite growth accelerated to **+391 test functions / +675 collected items in two days**, the fastest in the series; pytest wall 289.6 → 300.4s despite #5196 removing ~43s, and startup+collection (assertion rewriting) is now **58.6s of the 311.8s step**. (4) `entries-under-unreleased` failed on **13 distinct branches** in 12.5h, more red than every other check combined, because the 0.3.261 release moved the `## Unreleased` heading out from under open branches' entries. (5) Only **one `test`-leg failure in 200 runs**. Holding: **#5075** (reporting phase 0.2s, leg log 5,709 lines), **#4948** (ninth window, +6.3s imbalance, 97.3% efficiency), **#4760** (confirmed to survive `uv run`'s sync), **#4935** (no always-run step has a p90 above 2× its median anywhere in the snapshot), **#299** (three md/docs-only pushes, `test` legs 3–7s, Build wall 88s and 104s) **The 2026-09-05 run added:** the 2026-09-05 snapshot (200 PR runs over 17.6h, 2026-09-04 07:47 .. 2026-09-05 01:25 UTC), a full report rewrite, this ledger text, and **one safe fix** — `filter: "blob:none"` on the `Suppressions comment` checkout, the last unfiltered `fetch-depth: 0` left in any per-PR workflow (the other twelve were converted by #4848 and #4935, both from this skill). That job's own work is **1.0s**; its checkout is **28.0s median / 33.1s p90 of a 33.5s job**, and it runs once per PR push — **54 runs against 53 Build runs** this window, making it the most frequently executed job in the repository. Because it is `pull_request_target`, the collector cannot see it at all and all 20 job records were fetched by hand. Verified against a full clone of the same repo on four open upstream PRs (5215, 5248, 5234, 5233, chosen to cover both branches of the workflow's `merge_status` handling): **identical merged-tree OIDs and identical exit statuses, including the conflicted status-1 path**, and `git show <tree>:suppressions.json` resolves under both; driving `actions/checkout`'s own refspec end to end the job's git sequence goes **22.54s → 5.36s** and `.git` **421MB → 37MB**. A fresh-context review pass caught a **real defect the change would otherwise have introduced**: when the PR itself edits `suppressions.json`, the merged entry is the PR's own blob, which `merge-tree` never opens, so under `blob:none` the workflow's `git show "$merge_tree:suppressions.json"` becomes a *network* read — and its pre-existing `|| echo '{}'` fallback could not tell "absent" from "unfetchable", rendering a false **"788 → 0, all suppressions removed"** delta (reproduced at 392 lines, the exact outcome `suppressions_pr_delta.py`'s docstring says must be avoided). Fixed in the same commit: trees are always local under `blob:none`, so `git rev-parse --verify -q` answers "does this path exist" offline, the deletion fallback is preserved, and a failed promisor fetch now fails the job (verified: exit 128 under `GIT_NO_LAZY_FETCH=1`). Honest scope: this is **compute, not wall clock** — at 33.5s the job finishes long before Build's 360s, so no contributor waits on it; it removes ~20 of the ~30 runner-minutes per window it currently costs. **New findings this run:** (1) **`slow-tests (checkpoint)` reached the critical path** — 2 runs last window, **16 this one**, 362s median / 458s p90, and the last-finishing job in **7 of 45 successful Build runs by a 104s median margin** (52–151s), five times the 12–19s the `test` legs bind by; a checkpoint push costs **412s of Build wall against 352s** and **22.0 Build runner-min against 15.9**. The cause is one missing flag (`uv run pytest --runslow -m slow tests/<area>/`, no `-n`), and the sandbox reproduces CI exactly (20 passed / 11 skipped / 456 deselected, 446.2s local against 421.7s in job 101121776112). Under `-n 4` it runs in **224.2s (−50%)** but **4 tests fail**, all asserting on *global* Docker container state (`expected the killed attempt to leak its sandbox container / assert []` ×2; `Ctrl-C should tear down the sandbox; leaked ['51c0c3b0be60', 'c88ba6cfec9b']`; a container count of 4 where 2 was expected) — a genuine isolation defect, not flakiness, so **not shipped**; filed as [#444](https://github.com/meridianlabs-ai/inspect_ai/issues/444). (2) **#5220 landed 2026-09-03 21:48Z and its mechanism is confirmed** — 2 of the 17 `docs-render-*` keys created since the merge were re-read, both same-branch re-pushes 3 and 7 minutes apart, *exactly the case that had stopped hitting entirely* under the old key; and **0 of those 17 keys exist under both a PR ref and `refs/heads/main`** against **36 of 129** before, so the ~194 runner-min/week of provably identical renders is gone by construction. The hit *rate* is still undemonstrated — **0 of 7 `docs` jobs** in-window, but none of the seven was an eligible repeat (five first renders, one docs-only first push, one following a cancelled render that by design saves no marker). [#317](https://github.com/meridianlabs-ai/inspect_ai/issues/317) closed; evidence posted there. (3) **A 4-hour `pnpm/action-setup@v6` stall cost 64 runner-minutes** — 13 Viewer jobs between 07:47 and 11:48 UTC at **94–426s** against a 5–6s median, which is the entire Viewer workflow rise (133 → 202 runner-min, wall p90 100 → 129s); one incident, recorded not proposed. (4) **Queue returned to normal** — p90 57 → **5s**, max 157 → **67s**, waits over a minute 65 → **5** — but the burst was **not re-tested**: the densest 16-minute stretch was **24 runs** against the 47 that produced it, so this is absence of the trigger, not absorption of it. (5) **The cleanest window in the series: 3 failed job records in 200 runs**, none on a `test` leg; the previous window's changelog-relocation cluster (14 failures / 13 branches) fully cleared. (6) The collector's stale-page bug bit a **fifth** time (attempt 1: a 471h window with a 417h hole; a plain re-run was clean), and `.claude/**` is still unwritable, so its one-file fix stays blocked ([#298](https://github.com/meridianlabs-ai/inspect_ai/issues/298) updated — the `workflow` scope blocker cleared on 2026-09-03 is what made this run's fix shippable at all). Suite: **16,415 collected items** (+520 in two days) and **+184 test functions**, about half the previous window's rate; 3.10 worker time **906.7 → 875.5s**, the first two-day window in the series where measured test time fell. Holding: **#5075** (reporting 0.3s, leg log 5,899 lines), **#4948** (tenth window: +12.2s / 94.7% on 3.10, +2.7s / 98.9% on 3.11, no stragglers), **#4760** (11 `test_extensions` tests, 0.48s total), **#4935** (checkout 2–6s median in every job), **#299** (4 md-only pushes, Build wall 96–114s against 352s code-only, `mypy` binding all four). The **`uv sync` fix in this PR's first commit is still unmerged**, so `main` still pays it: the raw `test (3.10)` log of run 33936100151 shows `Uninstalled 50 packages … Installed 53 packages`, 5.4s. |
